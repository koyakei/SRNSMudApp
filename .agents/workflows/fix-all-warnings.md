---
name: fix-all-warnings
description: Analyze the C# project and fix all compiler warnings and style violations.
---
* Analyze the entire C# project and identify all compiler warnings and `.editorconfig` style violations.
* Safely remove all unused `using` directives across all `.cs` files.
* Fix minor code smells (e.g., unnecessary variables, unneeded type declarations).
* Apply the fixes directly and ensure the project builds successfully after your changes.
