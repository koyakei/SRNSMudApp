# エージェント作業ルール

このリポジトリで調査・実装・レビューを行うエージェントは、作業開始時に以下のルールを必ず確認すること。

## ルールの正本と優先順位

1. このファイル（リポジトリ全体の入口）
2. `.agents/AGENTS.md`（共通のアーキテクチャ・品質ルール）
3. `.agents/rules/mainRules.md`（常時適用のプロジェクトルール）
4. `.agents/rules/*.md`（分野別の詳細ルール）
5. 対象ディレクトリにある、より深い階層の `AGENTS.md`
6. `.editorconfig`、`qodana.yaml`、プロジェクト設定

同じ事項に異なる指示がある場合は、より対象範囲が狭いルールを優先する。ただし、セキュリティ、データ破壊防止、ユーザーの明示的な依頼に関する指示は、それらを優先して扱う。

## 必須確認事項

- BlazorのUI責務とドメインロジック・データアクセスを分離する。
- コンポーネントからの直接的なDBアクセスや、`IDialogService` の直接利用を避け、既定のサービス・ランチャー経由にする。
- 新規・変更コードでは警告、未使用 `using`、Roslynator/Qodanaの対象問題を増やさない。
- `dotnet format` を実行するときは、必ず `--diagnostics IDE0055` を指定する。引数なしのフル実行は禁止。
- I/O処理は非同期で実装し、Blazor Serverのライフサイクル、UI更新、イベント購読解除を考慮する。
- 変更には対応するテストを追加または更新し、可能な範囲で単体テストとE2Eテストを実行する。
- 一時スクリプトや生成物をリポジトリに残さない。

## 作業手順

1. 変更対象と適用される `AGENTS.md`・`.agents/rules/` を確認する。
2. 既存実装とテストを調査し、最小限の変更範囲を決める。
3. ルールに沿って実装し、変更ファイルの警告・スタイル問題を解消する。
4. 対応するテスト、ビルド、必要な静的解析を実行する。
5. 最終報告で、変更ファイル、検証結果、未解決事項を明示する。

詳細は、上記の正本ファイルを直接参照すること。内容をこのファイルに重複記載して独自解釈を増やしてはならない。
